<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mao.easyjokejava">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /><!-- 可选 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!--JobService权限-->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".BaseJokeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".WelcomeActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoadingActivity" />

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".test.FixedActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".test.ServiceActivity"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".test.SkinChangeActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".test.TestActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".test.DataBaseActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".test.ServiceProtectActivity"
            android:theme="@style/AppTheme" />

        <!--<service
            android:name=".test.MessageService"
            android:process=":remote" />-->

        <service android:name=".test.MessageService">
            <intent-filter>
                <action android:name="com.study.aidl.user" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>


        <service android:name=".test.service.MsgService" />

        <service
            android:name=".test.service.GuardService"
            android:process=":guardService" />

        <service
            android:name=".test.service.JobWakeUpService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>

</manifest>